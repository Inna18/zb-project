import BlogFixTemplate from '@/app/components/templates/BlogFixTemplate';
import React from 'react';

const page = () => {
  return (
    <div>
      <BlogFixTemplate />
    </div>
  );
};

export default page;
